# Historical Recovery Archive

Recovered from the local-only branch `docs/bidspace-repo-audit` during reconciliation on 2026-07-06.

This document is a historical snapshot, not the current source of truth.
Current repository code, issues, and integration branches supersede any stale findings below.

---
# Bidspace Repo Audit  ## 1. Executive Summary - BidSpace exists locally at `/home/jackson/automatedempires/ventures/bidspace` and is connected to `https://github.com/AutomatedEmpires/bidspace.git`. - The repo is a real monorepo, not an empty shell. It has a strong schema/core/services foundation plus a thin Next.js auth/onboarding shell. - Local install, lint, typecheck, package-level tests, and production build passed. - The app is not locally runnable without env setup: `pnpm --filter @bidspace/web dev` starts, but the first request returns HTTP 500 because `CLERK_PUBLISHABLE_KEY` is required at runtime. - Current maturity level: `prototype`. - Biggest strengths: canonical schema and decisions, clear package boundaries, real service-layer logic for discovery/bidding/payments/bookings, clean git alignment. - Biggest weaknesses: almost no marketplace UI, no shared component system, root test script is misleading, CI only partially verifies the monorepo, runtime depends on missing Clerk/Supabase env. - Highest-priority next steps: wire real monorepo verification, add local env/bootstrap documentation, ship the first marketplace routes/API, and establish a real UI/token system.  ### Marketplace / Product Readiness Scorecard  | Category | Score | Explanation | |---|---:|---| | Repo/GitHub alignment | 10/10 | Fresh clone on `main`, `origin/main`, ahead/behind `0/0`, clean before report files. | | Local setup quality | 7/10 | `.nvmrc`, `packageManager`, lockfile, and `.env.example` exist, but there is no complete local bootstrap flow for Clerk/Supabase/Doppler. | | Build health | 7/10 | `lint`, `typecheck`, package tests, and `build` pass; dev runtime still fails without env and root `test` is not real coverage. | | Product clarity | 8/10 | README and docs explain the product well and decisions are explicit. | | Route/page completeness | 3/10 | Only home, sign-in, sign-up, onboarding, onboarding completion, and dashboard exist. | | Component maturity | 2/10 | There is no shared UI package and almost no reusable local UI. | | Design system maturity | 1/10 | Styling is inline and token coverage is effectively absent. | | Data model maturity | 8/10 | The schema is substantial, well-documented, and backed by validation/state machines. | | Auth readiness | 6/10 | Clerk and org context are in place, but role-based product permissions are not yet surfaced or enforced through routes/API. | | Backend readiness | 7/10 | Domain services are meaningful, but there are no public API route handlers yet. | | Payment readiness | 5/10 | Split/payment logic exists, but live Stripe wiring and checkout UX are not present. | | Admin/moderation readiness | 2/10 | Trust/admin tables exist in schema, but no UI or workflows are implemented. | | Mobile UX | 1/10 | The app is not responsive and there is no mobile surface in the repo. | | Accessibility | 2/10 | Basic HTML exists, but there are no deliberate a11y patterns, states, or semantics beyond defaults. | | Security posture | 5/10 | Clerk auth and server-only helpers exist, but runtime uses the Supabase service role in app code and there is no hardened authorization layer yet. | | CI/CD readiness | 6/10 | One CI workflow exists and is green, but it does not fully verify all packages or deploy anything. | | Deployment readiness | 4/10 | Docs point to Vercel, but no deploy workflow, migration promotion path, or health checks are defined. | | Maintainability | 7/10 | Docs and package boundaries are strong; UI architecture is not. | | Scalability | 6/10 | Schema and services scale better than the current app shell, but operational and UI systems are immature. | | Overall MVP readiness | 4/10 | The core model is promising, but the repo is still several foundational slices away from a usable marketplace MVP. |  ## 2. Local Repo / GitHub Alignment  | Item | Value | |---|---| | Local path | `/home/jackson/automatedempires/ventures/bidspace` | | Remote URL | `https://github.com/AutomatedEmpires/bidspace.git` | | Current branch | `main` | | Default branch | `main` | | Ahead / behind | `0 / 0` vs `origin/main` | | Pre-audit status | Clean (`git status -sb` returned `## main...origin/main`) | | Uncommitted changes before report files | None | | Untracked files before report files | None | | Local/GitHub aligned | Yes | | Safe to pull | Yes, but unnecessary because the clone is already up to date | | Recommended next Git action | Create a feature branch from `main` before any product or infra changes, for example `git switch -c chore/bidspace-foundation-hardening` |  ## 3. Tech Stack  | Area | Current choice | |---|---| | Framework | Next.js 16 App Router in `apps/web` | | Language | TypeScript end to end | | Package manager | pnpm 10.12.4 | | Monorepo tooling | Turborepo | | Runtime | Node 24.16.0 | | Styling | Global CSS plus inline React style objects | | Backend / database | Supabase Postgres + PostGIS | | Auth | Clerk | | Payments | Stripe Connect design in services/docs; no live checkout flow yet | | Maps | Mapbox planned in docs; no UI integration yet | | Deployment target | Vercel per docs | | Observability | PostHog and Sentry documented, not integrated |  ## 4. Project Structure  - `apps/web`: the only app currently in the repo. Contains the Next.js routes, middleware, env helper, auth context, and simple dashboard/onboarding screens. - `packages/core`: canonical enums, Zod validation, money helpers, and state machines. - `packages/db`: SQL migrations, Supabase client wrapper, and hand-authored row contracts. - `packages/services`: transport-agnostic domain/service layer for venues, events, opportunities, inventory units, bidding, discovery, payments, and bookings. - `tools/github-notion-sync` and `tools/notion-github-issue-dispatcher`: workflow automation utilities unrelated to the product runtime. - `docs`: strong planning/reference docs including `ARCHITECTURE.md`, `DATA-MODEL.md`, `API.md`, `ROADMAP.md`, and `DECISIONS.md`.  Important config files:  | File | Purpose | |---|---| | `package.json` | Root scripts and runtime pinning | | `.nvmrc` | Pins Node 24.16.0 | | `pnpm-workspace.yaml` | Declares `apps/*` and `packages/*` workspaces | | `turbo.json` | Root task graph | | `.env.example` | Documents required env names | | `.github/workflows/ci.yml` | Current CI verification workflow | | `apps/web/next.config.ts` | Transpiles workspace packages into the Next app | | `apps/web/eslint.config.mjs` | Next-focused ESLint setup | | `tsconfig.base.json` | Shared TS strictness |  README quality: good on vision, stack, and architecture; overstated relative to the checked-in repo footprint because it references future or missing surfaces such as `apps/mobile`, `apps/api`, and `packages/ui`.  Environment setup quality: moderate. Runtime versions and env names are documented, but there is no single bootstrap path that gets a new machine from clone to a working local app.  ## 5. Existing Product Surfaces  | Route / page | File path | Status | Purpose | Notes | |---|---|---|---|---| | `/` | `apps/web/app/page.tsx` | placeholder | Minimal landing page with links into auth/dashboard | Static text only; no marketplace browse or product storytelling. | | `/sign-in/[[...sign-in]]` | `apps/web/app/sign-in/[[...sign-in]]/page.tsx` | complete | Clerk-hosted sign-in surface | Requires Clerk env; otherwise local runtime fails before render. | | `/sign-up/[[...sign-up]]` | `apps/web/app/sign-up/[[...sign-up]]/page.tsx` | complete | Clerk-hosted sign-up surface | Same env dependency as sign-in. | | `/onboarding` | `apps/web/app/onboarding/page.tsx` | partial | Lets an authenticated org choose marketplace roles | Real form and Clerk org selection exist, but styling/state handling are minimal. | | `/onboarding/complete` | `apps/web/app/onboarding/complete/route.ts` | partial | POST handler that syncs user, org membership, and role profiles into Supabase | Strong backend behavior, but no error UI and no idempotent UX feedback beyond redirect. | | `/dashboard` | `apps/web/app/dashboard/page.tsx` | partial | Shows current org linkage and role profiles | No marketplace dashboard, listings, bids, payments, analytics, or settings UI. |  Missing product surfaces that the repo evidence says are intended but not implemented yet:  - Marketplace discovery / browse map - Inventory unit detail page - Opportunity/listing CRUD screens - Bid submission and host bid pipeline UI - Booking/payment checkout UI - Profile/public role pages - Messaging/notifications UI - Reviews/verification/admin tools - Terms/privacy/about or other public trust pages  ## 6. Component Inventory  There is no local shared UI component system yet. `packages/ui` is referenced in docs but does not exist in the repo.  | Component / module | File path | Used where | Quality | Notes | |---|---|---|---|---| | `ClerkProvider` wrapper | `apps/web/app/layout.tsx` | Entire web app | adequate | Functional auth shell, but couples runtime startup to required env at module scope. | | `SignIn` | `apps/web/app/sign-in/[[...sign-in]]/page.tsx` | Sign-in route | adequate | Clerk-hosted; no local UX customization. | | `SignUp` | `apps/web/app/sign-up/[[...sign-up]]/page.tsx` | Sign-up route | adequate | Clerk-hosted; no local UX customization. | | `OrganizationList` | `apps/web/app/onboarding/page.tsx` | Onboarding without active org | adequate | Useful starter surface, but not wrapped or themed. | | `OrganizationSwitcher` | `apps/web/app/dashboard/page.tsx` | Dashboard header | adequate | Functional, but no surrounding navigation shell. | | `UserButton` | `apps/web/app/dashboard/page.tsx` | Dashboard header | adequate | Same limitation as `OrganizationSwitcher`. | | Role selection form | `apps/web/app/onboarding/page.tsx` | Onboarding | weak | Inline, one-off HTML. No loading/error/empty states, no extracted form component. | | `getCurrentUserOrgContext` | `apps/web/lib/auth-context.ts` | Dashboard | good | Useful reusable server helper, but built on service-role DB access. | | `normalizeMembershipRole` / `hasOrgRole` | `apps/web/lib/permissions.ts` | Onboarding and future authz | good | Small, focused logic with passing tests. | | `createServerBidspaceClient` | `apps/web/lib/bidspace-server.ts` | Onboarding/dashboard auth context | mixed | Useful abstraction, but it always uses `SUPABASE_SERVICE_ROLE_KEY`, which widens blast radius. |  Component concerns:  - Reusability: low overall because UI is page-local and inline. - Duplication: inline `padding`, `fontFamily`, header layout, and form styling patterns are already repeated. - Loading/error/empty states: mostly missing except a single empty-state paragraph in the dashboard. - Accessibility: no deliberate form system, focus states, keyboard patterns, or ARIA design.  ## 7. Design Tokens  | Token area | Current state | |---|---| | Colors | `--background` and `--foreground` in `apps/web/app/globals.css`, plus hard-coded page colors like `#ddd` and `#b91c1c` | | Fonts | `Arial, Helvetica, sans-serif` | | Font sizes | Browser defaults only | | Font weights | Browser defaults only | | Line heights | Browser defaults only | | Spacing scale | None; raw numeric inline values such as `24`, `16`, `12`, `8` | | Border radius | None defined | | Shadows | None defined | | Breakpoints | None defined | | Motion / animation | None | | z-index | None | | Button variants | Native button only | | Badge/chip variants | None | | Card variants | None | | Input styles | Native checkbox + button only | | Navigation styles | None | | Dark mode | Basic `prefers-color-scheme` switch in global CSS |  Design system assessment:  - Consistent: no - Inconsistent: yes - Tokenized: barely - Hard-coded: yes - Duplicated: yes - Scalable: no - Production-grade: no  There is no Tailwind config, no CSS variable set beyond foreground/background, no shadcn/Radix setup, no icon implementation, and no shared component variants.  ## 8. Data Model  Canonical source of truth: `packages/db/migrations/*.sql`  | Entity | Key fields | Canonical source | Used in | Gaps / notes | |---|---|---|---|---| | User | `id`, `auth_provider_id`, `email`, `full_name`, `status` | `packages/db/migrations/0003_identity.sql` | onboarding sync, auth context | Strong schema; app uses Clerk user id mapping. | | Organization | `id`, `name`, `organization_type`, `verification_status`, `stripe_account_id` | `packages/db/migrations/0003_identity.sql` | onboarding, dashboard, payments | Multi-role org model is clear and consistent. | | OrganizationMembership | `organization_id`, `user_id`, `role`, `status` | `packages/db/migrations/0003_identity.sql` | onboarding | Good base for RBAC, but route/API enforcement is still light. | | RoleProfile | `organization_id`, `role_type`, `display_name`, `slug`, `category_tags` | `packages/db/migrations/0003_identity.sql` | onboarding, dashboard | Current UI only creates display names and empty tags. | | Venue | location, address, capacity, verification fields | `packages/db/migrations/0004_locations.sql` | services/docs only | No web/API surface yet. | | VenueZone | `venue_id`, boundary, center, floorplan coords, scores | `packages/db/migrations/0004_locations.sql` | schema/search design | Not represented in `packages/db/src/types.ts`. | | Event | timing, type, attendance fields | `packages/db/migrations/0004_locations.sql` | services/docs only | No route/UI yet. | | InventoryCollection | owner, name, collection type, timing | `packages/db/migrations/0004_locations.sql` | docs/schema only | Not represented in `packages/db/src/types.ts`. | | Opportunity | title, commerce layer, pricing mode, bid deadline, location | `packages/db/migrations/0005_marketplace.sql` | services/docs only | Canonical concept is solid; no UI/API yet. | | InventoryUnit | type, pricing, geospatial location, floorplan, utilities | `packages/db/migrations/0005_marketplace.sql` | services/search docs | Core marketplace primitive; strong schema and search RPC support. | | Bid | bidder/host orgs, amount, status, counter, intended use | `packages/db/migrations/0006_transactions.sql` | bidding service/tests | Strong state machine and sealed visibility logic. | | BidPreference | unit, rank, amount, criteria | `packages/db/migrations/0006_transactions.sql` | schema only | Not modeled in app/UI yet. | | Booking | bid, unit, timing, price, status | `packages/db/migrations/0006_transactions.sql` | booking service | Backend logic exists; no UI or checkout flow. | | Payment | booking, payer/payee, fee split, Stripe intent, status | `packages/db/migrations/0006_transactions.sql` | payments service | Connect split logic exists without live adapter wiring. | | MessageThread / Message | context, sender, body, attachments | `packages/db/migrations/0007_trust.sql` | schema only | No route/UI or TS row contracts. | | Review | rating dimensions, feedback, `would_book_again` | `packages/db/migrations/0007_trust.sql` | schema only | No product surface yet. | | Document | owner, `file_url`, type, status, expiry | `packages/db/migrations/0007_trust.sql` | schema only | No product surface yet. | | Verification | subject, type, status, evidence ids, risk score | `packages/db/migrations/0007_trust.sql` | schema only | No product surface yet. | | PerformanceData | metric key/value/text, source, category | `packages/db/migrations/0007_trust.sql` | schema only | Future moat data, not yet wired. | | AdminAction | admin user, action type, target | `packages/db/migrations/0007_trust.sql` | schema only | Admin tooling not started. |  Type-safety and normalization notes:  - `packages/core/src/validation.ts` and `packages/core/src/state-machines.ts` provide a good canonical domain layer. - `packages/db/src/types.ts` is intentionally hand-authored and partial. It does not cover every table in the schema, so drift risk remains until generated types are adopted. - `packages/db/src/client.ts` is intentionally schema-untyped at the Supabase client level.  ## 9. Build / Lint / Test Results  Commands run and outcomes:  | Command | Result | Notes | |---|---|---| | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use && node --version && pnpm --version && pnpm install --frozen-lockfile` | passed | Lockfile was current. pnpm warned that build scripts were ignored and the `supabase` bin symlink could not be created. | | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use >/dev/null && pnpm lint && pnpm typecheck && pnpm test && pnpm build` | passed with caveats | Root `test` only prints `(no tests yet)`. `build` passed but emitted the Next 16 `middleware` deprecation warning. | | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use >/dev/null && pnpm --filter @bidspace/core test` | passed | 6/6 tests passed. | | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use >/dev/null && pnpm --filter @bidspace/services test` | passed | 17/17 tests passed. | | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use >/dev/null && pnpm --filter @bidspace/web test` | passed | 2/2 tests passed. | | `cd /home/jackson/automatedempires/ventures/bidspace && source ~/.nvm/nvm.sh && nvm use >/dev/null && pnpm --filter @bidspace/web dev` | started | Next dev server was ready on port 3000. | | `curl -I http://127.0.0.1:3000` | failed | Returned HTTP 500 because runtime evaluation threw `Missing required environment variable: CLERK_PUBLISHABLE_KEY`. |  Build health summary:  - Install: healthy enough for local work, but package-manager script approval and Supabase CLI bin warnings should be cleaned up. - Lint: only the web app is linted right now; `core`, `db`, and `services` do not expose lint scripts. - Typecheck: green across packages. - Tests: real tests exist and pass, but root CI is not invoking them. - Build: green for the web app, but the runtime story is not green without env. - Dev smoke test: server boots, first request fails without Clerk env.  Likely root causes of the current runtime issue:  - `apps/web/app/layout.tsx` calls `getRequiredEnv("CLERK_PUBLISHABLE_KEY")` during runtime. - `apps/web/middleware.ts` calls `getRequiredEnv` for both Clerk keys during startup. - `apps/web/lib/env.ts` intentionally allows build-time placeholders, which keeps CI green but leaves runtime misconfiguration to fail on first request.  Current repo state:  - Currently buildable: yes - Currently runnable locally without env setup: no - Currently deployable as-is: partially, but not safely or reproducibly from the repo alone  ## 10. GitHub Actions / CI-CD  Existing workflows:  | Workflow | Trigger | What it does | Gap | |---|---|---|---| | `.github/workflows/ci.yml` | `push` to `main`, `pull_request`, `merge_group` | Sets up pnpm/Node, installs deps, runs root `typecheck`, `lint`, `build`, `test` | Root scripts under-report actual monorepo health and there is no deploy job |  What the current CI really verifies:  - Typecheck across packages - Web-only lint - Web-only build - Placeholder root test script instead of package tests  What is missing:  - Frozen-lockfile enforcement in CI - Real unit test execution from the root - Migration validation against a database target - Preview deployment - Production deployment - Post-deploy smoke checks - Scheduled dependency/security audit  Recommended professional CI pipeline:  1. `verify.yml`: `pnpm install --frozen-lockfile`, lint all workspaces, typecheck all workspaces, run package tests, build the web app. 2. `db-verify.yml`: boot Postgres/Supabase, apply migrations, and run schema smoke checks. 3. `preview.yml`: deploy preview on pull requests and run a health check. 4. `release.yml`: deploy from `main`, run migrations in order, then perform a post-deploy smoke test. 5. `security.yml`: scheduled `pnpm audit`, Dependabot or Renovate, and secret scanning policy.  ## 11. Environment Variables / Secrets  Required env var names documented in `.env.example`:  - Database: `DATABASE_URL`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` - Auth: `CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY` - Payments: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_CONNECT_CLIENT_ID` - Maps: `MAPBOX_ACCESS_TOKEN`, `NEXT_PUBLIC_MAPBOX_TOKEN` - Media: `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` - Analytics/monitoring: `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, `SENTRY_DSN`, `SENTRY_AUTH_TOKEN` - AI: `OPENAI_API_KEY`  Audit findings:  - No real secret values were exposed during this audit. - No committed `.env` or `.env.local` file was found in the repo root. - Tooling-specific `.env.example` files exist under `tools/github-notion-sync` and `tools/notion-github-issue-dispatcher`. - Secret naming is generally clear. - Local setup is not fully reproducible yet because the repo references Doppler in comments/docs but does not include a checked-in Doppler bootstrap/config path.  Risk notes:  - The current web runtime requires Clerk and Supabase env immediately. - `SUPABASE_SERVICE_ROLE_KEY` is used from app server code, which should be treated as a sensitive/high-blast-radius secret. - I found no evidence of committed live secrets, so there is no immediate rotation recommendation from the checked-in files alone.  ## 12. Strengths  - Strong canonical decision log and product framing in `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`, and `README.md`. - High-quality schema foundation in `packages/db/migrations/0003_identity.sql` through `0008_search.sql`. - Good domain rigor in `packages/core/src/validation.ts`, `packages/core/src/state-machines.ts`, and `packages/core/src/money.ts`. - Meaningful service layer in `packages/services/src/discovery.ts`, `packages/services/src/bidding.ts`, `packages/services/src/payments.ts`, and `packages/services/src/booking.ts`. - Clean local/GitHub alignment and pinned runtime via `.nvmrc`, `package.json`, and `pnpm-lock.yaml`.  ## 13. Weaknesses  - User-facing product surface is extremely thin in `apps/web/app/*`. - Shared UI/design system is effectively absent; styling is inline in `apps/web/app/page.tsx`, `apps/web/app/onboarding/page.tsx`, and `apps/web/app/dashboard/page.tsx`. - Runtime boot depends on env without a complete local setup path in `.env.example` plus docs. - The root quality gate is misleading because `package.json` defines `test` as `echo "(no tests yet)"` even though package tests exist. - The root lint/build scripts do not cover all workspaces; `packages/core`, `packages/db`, and `packages/services` are not linted and do not build from the root. - `packages/services/README.md` is already stale relative to the shipped service surface. - `packages/db/src/types.ts` only partially mirrors the schema and will drift without generation.  ## 14. Product Gaps  What is missing for a serious MVP:  - Real discovery/browse experience for inventory units - Inventory unit detail and bid entry flows - Host tools for venues, opportunities, inventory units, and bid review - Booking/payment checkout and success/failure states - Messaging, review, verification, and admin experiences - Profile and public trust surfaces - Polished dashboard/navigation structure  The repo has the domain model for these surfaces, but not the user journeys.  ## 15. Design System Gaps  - No token source for typography, spacing, radius, shadows, z-index, or motion - No shared button/input/card/navigation primitives - No icon implementation despite a documented icon strategy - No responsive layout system - No accessibility baseline for forms, focus, or state messaging - No visual distinction yet between host, bidder, and admin surfaces  ## 16. Architecture Risks  - Runtime env risk: build-time placeholders hide missing runtime config until the first request. - Authorization risk: server code uses the Supabase service role key broadly instead of a narrower per-request access model. - Verification risk: CI green status overstates reality because root `test` is a placeholder and root lint is web-only. - Documentation drift: README/docs describe a fuller app set than currently exists, and `packages/services/README.md` understates the current service implementation. - Scalability risk on the frontend: inline styles and page-local UI will create churn once discovery/dashboard surfaces are added. - Deployment risk: there is no repository-owned deployment, migration promotion, or health-check workflow.  ## 17. Priority Roadmap  ### Immediate Fixes - Make root `pnpm test` run the actual package tests. - Add lint scripts for `packages/core`, `packages/db`, and `packages/services`. - Document a working local bootstrap path for Clerk/Supabase/Doppler. - Replace deprecated Next `middleware` convention with `proxy` before Next 17+ pressure increases.  ### Foundation Fixes - Generate DB types from Supabase and retire partial hand-authored row coverage. - Introduce `packages/ui` plus a token system. - Add API route handlers that wrap the service layer with authz/error mapping. - Tighten authz boundaries around organization membership and service-role usage.  ### Product Buildout - Ship discovery/map and list surfaces first, because the domain/search layer is already present. - Add inventory unit detail plus bid submission. - Add host bid pipeline, booking, and payment completion screens. - Add messaging/reviews/admin only after the core marketplace loop is real.  ### Production Readiness - Stand up preview and production deploy workflows. - Add migration verification and promotion discipline. - Add observability, error boundaries, and health checks. - Harden secret/bootstrap management and document environment ownership.  ## 18. Recommended File/Folder Refactor  Recommendation only, based on the current repo evidence:  ``` apps/   web/     app/       (public)/       (auth)/       (app)/dashboard/       (app)/inventory/       (app)/bids/       api/     components/     features/     lib/ packages/   core/   db/   services/   ui/     src/primitives/     src/patterns/     src/tokens/   config/ ```  Why:  - Keep routes thin and feature-oriented. - Separate reusable primitives/patterns from app-specific features. - Make token ownership explicit. - Give API handlers and feature modules a clear home before more routes arrive.  ## 19. Recommended Domain Model  Recommendation only, based on the current schema and docs:  ```ts type OrganizationId = string; type UserId = string; type OpportunityId = string; type InventoryUnitId = string; type BidId = string; type BookingId = string;  interface Organization {   id: OrganizationId;   name: string;   organizationType: "multi" | "host" | "bidder" | "venue_owner" | "sponsor";   verificationStatus: string;   stripeAccountId: string | null; }  interface RoleProfile {   id: string;   organizationId: OrganizationId;   roleType: string;   displayName: string;   slug: string | null;   categoryTags: string[]; }  interface Opportunity {   id: OpportunityId;   organizationId: OrganizationId;   venueId: string | null;   eventId: string | null;   title: string;   pricingMode: string;   commerceLayer: string | null;   minimumBidCents: number | null;   bidDeadline: string | null;   status: string; }  interface InventoryUnit {   id: InventoryUnitId;   opportunityId: OpportunityId;   organizationId: OrganizationId;   type: string;   status: string;   pricingMode: string;   minimumBidCents: number | null;   buyNowPriceCents: number | null;   reservePriceCents: number | null;   latitude?: number;   longitude?: number; }  interface Bid {   id: BidId;   bidderOrganizationId: OrganizationId;   hostOrganizationId: OrganizationId | null;   opportunityId: OpportunityId;   inventoryUnitId: InventoryUnitId | null;   amountCents: number;   counterAmountCents: number | null;   status: string; }  interface Booking {   id: BookingId;   bidId: BidId;   inventoryUnitId: InventoryUnitId;   bidderOrganizationId: OrganizationId;   hostOrganizationId: OrganizationId;   priceCents: number;   status: string; }  interface Payment {   id: string;   bookingId: BookingId;   amountCents: number;   platformFeeCents: number | null;   hostPayoutCents: number | null;   status: string;   stripePaymentIntentId: string | null; } ```  Recommendation notes:  - Promote a generated canonical type layer from the database schema instead of maintaining partial row contracts by hand. - Keep money fields in cents and preserve the existing state-machine pattern. - Keep organization-mediated ownership as the cross-cutting boundary for authz.  ## 20. Final Verdict  Repo state: `prototype`  Why:  - The data model, decisions, and services are well ahead of a normal prototype. - The actual user-facing product is still only auth, onboarding, and a placeholder dashboard. - Runtime setup and CI do not yet represent a professional, reproducible marketplace stack.  Best next move:  - Harden the monorepo quality gates and local bootstrap path, then ship the first real marketplace slice: discovery/listing plus unit detail plus bid submission.  What should not be built yet:  - Advanced AI, enterprise analytics, broad admin tooling, or secondary product surfaces before the core discovery-to-bid flow is real.  What must be standardized before scaling:  - Root verification scripts - UI tokens/components - API/authz boundaries - Generated DB types - Environment/bootstrap and deployment workflows# Bidspace Repo Audit  ## 1. Executive Summary - Bidspace exists locally and on GitHub as a pnpm/Turborepo monorepo with one live Next.js web app at `apps/web`, plus substantive `core`, `db`, and `services` packages. - GitHub remote is connected to `https://github.com/AutomatedEmpires/bidspace.git`; before this audit file was written the local clone was clean, on `main`, and exactly aligned with `origin/main`. - The repo installs cleanly with the pinned runtime, passes root lint, typecheck, and production build, and package-level tests pass when run directly. - The app does not fully run locally without secrets: `pnpm --filter @bidspace/web dev` starts, but the first request returns HTTP 500 because `CLERK_PUBLISHABLE_KEY` is required at runtime from `apps/web/lib/env.ts` and is read by `apps/web/middleware.ts`. - Current maturity is best described as a prototype with a strong domain foundation. The database schema, validation, state machines, discovery services, bidding services, and payment split logic are materially ahead of the actual product UI. - Biggest strengths: canonical schema discipline in `packages/db/migrations`, strong business-rule enforcement in `packages/core` and `packages/services`, clear documentation in `docs`, and a clean monorepo/runtime baseline. - Biggest weaknesses: almost no product surface beyond auth/onboarding, no custom reusable UI layer, root test orchestration is misleading, and local runtime depends on secrets with no non-secret development fallback. - Highest-priority next steps: wire the real package tests into root CI, build the first marketplace surfaces on top of the existing service layer, create a tokenized UI system, and make local development reproducible with documented secret provisioning.  ## 2. Local Repo / GitHub Alignment  | Item | Value | |---|---| | Local path | `/home/jackson/automatedempires/ventures/bidspace` | | Remote URL | `https://github.com/AutomatedEmpires/bidspace.git` | | Current branch | `main` | | Default branch | `main` | | Ahead / behind | `0 / 0` | | Pre-audit status | `## main...origin/main` | | Uncommitted changes before reports | None | | Untracked files before reports | None | | Local Γåö GitHub alignment | Fully aligned at audit start | | Safe to pull | Yes, based on the pre-audit clean state | | Recommended next Git action | Create a feature branch for the first corrective PR; do not work directly on `main` |  Evidence gathered with:  ```bash cd ~/automatedempires/ventures/bidspace pwd git status git remote -v git branch --show-current git branch -a git log --oneline --decorate -n 10 git fetch --all --prune git status -sb git rev-list --left-right --count HEAD...origin/main ```  ## 3. Tech Stack  | Layer | Current choice | Evidence | |---|---|---| | Framework | Next.js 16 App Router | `apps/web/package.json`, `apps/web/app/*` | | Language | TypeScript | root and package `tsconfig` files | | Runtime | Node 24.16.0 | `.nvmrc`, root `package.json` | | Package manager | pnpm 10.12.4 | `packageManager`, `pnpm-lock.yaml` | | Monorepo tooling | Turborepo | `turbo.json` | | Auth | Clerk | `apps/web/app/layout.tsx`, `apps/web/middleware.ts`, docs | | Database | Supabase Postgres + PostGIS | `packages/db`, docs | | Payments | Stripe Connect, service-layer only | `packages/services/src/payments.ts`, docs | | Maps | Mapbox planned, not yet integrated in UI | `.env.example`, docs | | Styling | Global CSS plus inline styles | `apps/web/app/globals.css`, route files | | Deployment target | Vercel by documentation, not automated in repo | `docs/ARCHITECTURE.md`, `AGENTS.md` | | Observability | PostHog + Sentry documented, not wired | `.env.example`, `docs/INTEGRATIONS.md` |  ## 4. Project Structure  Top-level structure is lean and readable:  - `apps/web`: only shipped product surface. Contains a minimal App Router shell, Clerk auth pages, onboarding flow, dashboard, middleware, and server env helpers. - `packages/core`: canonical enums, zod validation, money helpers, and state machines. - `packages/db`: schema source of truth, Supabase client factory, and hand-authored row interfaces. - `packages/services`: transport-agnostic business logic for venues, events, opportunities, inventory units, discovery, bidding, payments, and bookings. - `docs`: unusually strong canonical documentation for architecture, API, decisions, data model, roadmap, and prior audits. - `tools`: Notion/GitHub sync workers, ancillary to the main product runtime.  Important config files:  | File | Purpose | Assessment | |---|---|---| | `package.json` | Root runtime + workspace scripts | Clean, but `test` is misleading placeholder | | `pnpm-workspace.yaml` | Workspace package inclusion | Minimal and correct | | `turbo.json` | Pipeline wiring | Valid, but `typecheck` depends on `^build` even where packages have no build script | | `tsconfig.base.json` | Shared strict TS defaults | Strong baseline | | `apps/web/next.config.ts` | Transpiles shared packages | Minimal and adequate | | `apps/web/eslint.config.mjs` | Next + TS lint config | Minimal, acceptable | | `.env.example` | Secret names and local DB URL | Good names; incomplete reproducibility without Doppler/Vercel setup doc | | `.github/workflows/ci.yml` | Install, typecheck, lint, build, test | Good base, but test coverage is understated and no deploy steps exist |  README quality: good. The root README clearly states the product, stack, runtime, canonical docs, and repo layout. Environment setup quality: partial. Variable names are documented, but the actual secret hydration path is only implied via Doppler and not fully reproducible from the repo alone.  ## 5. Existing Product Surfaces  | Route/page | File path | Status | Purpose | Notes | |---|---|---|---|---| | `/` | `apps/web/app/page.tsx` | Placeholder | Minimal landing page linking into auth and dashboard | Static copy only: ΓÇ£Phase 3 auth scaffold is ready.ΓÇ¥ | | `/sign-in/[[...sign-in]]` | `apps/web/app/sign-in/[[...sign-in]]/page.tsx` | Complete | Clerk sign-in entry | Uses Clerk hosted UI; no local branding or content | | `/sign-up/[[...sign-up]]` | `apps/web/app/sign-up/[[...sign-up]]/page.tsx` | Complete | Clerk sign-up entry | Same hosted UI pattern | | `/onboarding` | `apps/web/app/onboarding/page.tsx` | Partial | Create/select organization context and choose marketplace roles | Functional HTML form; no polished UX, no loading/error affordances | | `/onboarding/complete` | `apps/web/app/onboarding/complete/route.ts` | Complete backend step | Sync Clerk user/org into Bidspace DB, upsert membership and role profiles, redirect to dashboard | This is a route handler, not a user-facing page | | `/dashboard` | `apps/web/app/dashboard/page.tsx` | Partial | Show active org linkage and role profiles | Protected; no inventory, bids, bookings, discovery, or admin capabilities | | Protected route shell | `apps/web/middleware.ts` | Partial infrastructure | Protect dashboard/onboarding and inject org header | Uses deprecated Next middleware convention warning in Next 16 |  Actual product surfaces missing from the running app, despite service/documentation support:  - Marketplace browse or map page - Inventory unit detail page - Opportunity creation/edit flow - Bid creation and bid pipeline UI - Booking management UI - Payments/checkout UI - Messaging UI - Reviews, verification, moderation, and admin UI - Profile/public organization surfaces - Terms, privacy, about, contact pages  ## 6. Component Inventory  There is no local reusable UI component library yet.  | Component | File path | Used where | Quality | Notes | |---|---|---|---|---| | `ClerkProvider` wrapper | `apps/web/app/layout.tsx` | Entire web app | Adequate | Provides auth, but ties runtime boot to env availability | | `SignIn` | `apps/web/app/sign-in/[[...sign-in]]/page.tsx` | Sign-in route | Good external component | Hosted by Clerk; no local abstraction | | `SignUp` | `apps/web/app/sign-up/[[...sign-up]]/page.tsx` | Sign-up route | Good external component | Hosted by Clerk | | `OrganizationList` | `apps/web/app/onboarding/page.tsx` | Onboarding when no org is active | Good external component | Functional, not customized | | `OrganizationSwitcher` | `apps/web/app/dashboard/page.tsx` | Dashboard header | Good external component | Useful but not abstracted behind local layout shell | | `UserButton` | `apps/web/app/dashboard/page.tsx` | Dashboard header | Good external component | Same issue | | Role selection form | `apps/web/app/onboarding/page.tsx` | Onboarding | Weak | One-off inline form, hard-coded layout, no reusable field primitives |  Shared logic modules with reuse value:  | Module | File path | Used where | Quality | Notes | |---|---|---|---|---| | `getRequiredEnv` | `apps/web/lib/env.ts` | layout, middleware, server client | Strong but strict | Keeps builds green with placeholders; causes runtime failure without secrets | | `getCurrentUserOrgContext` | `apps/web/lib/auth-context.ts` | dashboard | Good | Useful auth/data bridge, but only serves one route today | | `normalizeMembershipRole` / `hasOrgRole` | `apps/web/lib/permissions.ts` | onboarding and tests | Good | Small, reusable permission helper |  Inventory assessment:  - Reusability: low on UI, moderate on backend modules. - Duplication: inline page chrome and typography are already duplicated across the few pages that exist. - Hard-coded styles: pervasive in page files. - Missing loading/error/empty states: dashboard has one empty state message, otherwise missing. - Accessibility concerns: no app-level button/input primitives, no focus styles, no fieldset error semantics beyond plain text, no explicit keyboard/accessibility review.  ## 7. Design Tokens  Current visual system findings:  | Token area | Current state | |---|---| | Colors | Only `--background` and `--foreground` in `apps/web/app/globals.css`; inline colors include `#ddd` and `#b91c1c` | | Fonts | `Arial, Helvetica, sans-serif` | | Font sizes | Browser defaults only | | Font weights | Browser defaults only | | Line heights | Browser defaults only | | Spacing scale | None; ad hoc inline values like `24`, `16`, `12`, `8` | | Border radius | None standardized | | Shadows | None | | Breakpoints | None | | Animation / motion | None | | z-index | None | | Button variants | Native button only | | Badge / chip variants | None | | Card variants | None | | Input styles | Native checkbox only | | Navigation styles | None beyond text links | | Dark mode support | Minimal foreground/background swap via `prefers-color-scheme` |  Assessment:  - Consistent: no - Inconsistent: yes - Tokenized: minimally, only two CSS variables - Hard-coded: yes - Duplicated: yes, via repeated inline page styles - Scalable: no - Production-grade: no  This repo currently has no Tailwind config, no `packages/ui`, no variant system, and no icon integration even though Streamline is the documented standard.  ## 8. Data Model  Canonical schema lives in `packages/db/migrations`. The data model is the strongest part of the repository.  | Entity | Key fields | Canonical source | Used in | |---|---|---|---| | User | `id`, `auth_provider_id`, `email`, `full_name`, `status` | `packages/db/migrations/0003_identity.sql` | onboarding sync, auth context | | Organization | `id`, `name`, `organization_type`, `status`, `verification_status`, `stripe_account_id` | `packages/db/migrations/0003_identity.sql` | onboarding, dashboard, payments | | OrganizationMembership | `organization_id`, `user_id`, `role`, `status` | `packages/db/migrations/0003_identity.sql` | onboarding, permission helpers | | RoleProfile | `organization_id`, `role_type`, `display_name`, `slug`, `category_tags`, `verification_status` | `packages/db/migrations/0003_identity.sql` | onboarding, dashboard | | Venue | name/location/capacity/logistics fields | `packages/db/migrations/0004_locations.sql` | services layer | | VenueZone | polygon/centroid/floorplan coordinates | `packages/db/migrations/0004_locations.sql` | schema only today | | Event | `event_type`, dates, attendance | `packages/db/migrations/0004_locations.sql` | services layer | | InventoryCollection | grouping metadata | `packages/db/migrations/0004_locations.sql` | schema/docs only today | | Opportunity | title, pricing mode, commerce layer, min bid, deadline | `packages/db/migrations/0005_marketplace.sql` | services layer | | InventoryUnit | type, availability, geo fields, pricing, logistics, scores | `packages/db/migrations/0005_marketplace.sql` | services layer, discovery RPCs | | Bid | bidder/host orgs, amount, status, counter, intended use | `packages/db/migrations/0006_transactions.sql` | bidding service | | BidPreference | ranked fallback preferences | `packages/db/migrations/0006_transactions.sql` | schema/docs only today | | Booking | bid lock, unit lock, schedule, price, status | `packages/db/migrations/0006_transactions.sql` | booking service | | Payment | amount, fee split, payout, Stripe intent id, status | `packages/db/migrations/0006_transactions.sql` | payments service | | MessageThread / Message | messaging context and body | `packages/db/migrations/0007_trust.sql` | schema only today | | Review | rating and qualitative feedback | `packages/db/migrations/0007_trust.sql` | schema only today | | Document | verification/supporting file metadata | `packages/db/migrations/0007_trust.sql` | schema only today | | Verification | subject/type/status/risk/expires | `packages/db/migrations/0007_trust.sql` | schema only today | | PerformanceData | unit/opportunity/booking metrics | `packages/db/migrations/0007_trust.sql` | schema only today | | AdminAction | moderation/audit log | `packages/db/migrations/0007_trust.sql` | schema only today |  Duplication and type-safety notes:  - Canonical: SQL migrations are canonical. - TypeScript duplication exists intentionally in `packages/db/src/types.ts`, which is hand-authored and explicitly temporary until `supabase gen types` is used. - Some row interfaces are partial compared with the full schema. Example: `RoleProfileRow` omits several schema fields such as `status`, `verification_status`, and gallery/contact metadata. - Services and zod validation are generally aligned with the current schema and decisions. - Normalization is strong in the schema; gaps are mostly UI/API absence rather than model weakness.  ## 9. Build / Lint / Test Results  Commands actually run:  ```bash cd ~/automatedempires/ventures/bidspace if [[ -s "$HOME/.nvm/nvm.sh" ]]; then source "$HOME/.nvm/nvm.sh" && nvm use; fi node --version pnpm --version pnpm install --frozen-lockfile pnpm lint pnpm typecheck pnpm test pnpm build pnpm --filter @bidspace/core test pnpm --filter @bidspace/services test pnpm --filter @bidspace/web test pnpm --filter @bidspace/web dev curl -I http://127.0.0.1:3000 ```  Results:  | Check | Result | Notes | |---|---|---| | Install | Pass with warnings | Lockfile was current; pnpm warned that build scripts were ignored and `supabase` bin links could not be created | | Root lint | Pass | Only `@bidspace/web` has a lint script today | | Root typecheck | Pass | `core`, `db`, `services`, and `web` all passed | | Root test | Misleading pass | Script is `echo "(no tests yet)"`; it does not execute actual package tests | | Root build | Pass | Next production build completed; route output limited to auth/onboarding/dashboard | | `@bidspace/core` tests | Pass | 6/6 passed | | `@bidspace/services` tests | Pass | 17/17 passed | | `@bidspace/web` tests | Pass | 2/2 passed | | Dev startup | Partial pass | Next dev server started on port 3000 | | Dev smoke request | Fail | HTTP 500 on `/` because `CLERK_PUBLISHABLE_KEY` was missing at runtime |  Main errors / warnings:  - `apps/web/middleware.ts` triggers a Next 16 deprecation warning: middleware file convention should move to proxy. - Local runtime requires Clerk env immediately; `apps/web/lib/env.ts` only uses placeholders during production build phase, not during dev server execution. - The install produced `supabase` bin-link warnings, likely because build scripts were ignored by pnpm. This did not block the app build, but it is a setup risk for local schema tooling. - CI runs `pnpm install --frozen-lockfile=false`, which weakens dependency reproducibility.  Current health summary:  - Currently runnable: partially. Production build succeeds; local interactive runtime requires secrets. - Currently deployable: partially. The web build artifact is healthy, but there is no deployment workflow, no documented environment promotion path, and no functional marketplace UI.  ## 10. GitHub Actions / CI-CD  Existing workflows:  | Workflow | Trigger | What it does | Assessment | |---|---|---|---| | `.github/workflows/ci.yml` | `push` to `main`, `pull_request`, `merge_group` | checkout, setup pnpm/node, install, typecheck, lint, build, test | Good base verification, but incomplete for real quality gates |  What CI currently misses:  - Root `pnpm test` does not run the actual package tests. - No deployment automation. - No dependency audit or supply-chain check. - No migration validation or schema smoke test. - No preview environment verification. - No post-deploy health checks.  Secrets referenced in CI: none directly in the current workflow.  Branch protection cannot be proven from repo contents alone. The presence of PR and merge-group triggers suggests the repo is intended to support stricter protections, but that is a GitHub settings concern rather than a file-level fact.  Recommended professional CI pipeline:  1. `ci.yml`: install with frozen lockfile, lint, typecheck, package tests, build. 2. `db-verify.yml`: validate SQL migrations and generate/check DB types. 3. `preview.yml`: create preview deployment for PRs and run smoke tests. 4. `deploy-production.yml`: gated deploy from `main` after successful verification. 5. `deps-audit.yml`: scheduled dependency and security audit.  ## 11. Environment Variables / Secrets  Required variable names documented in `.env.example`:  - `DATABASE_URL` - `SUPABASE_URL` - `SUPABASE_ANON_KEY` - `SUPABASE_SERVICE_ROLE_KEY` - `CLERK_PUBLISHABLE_KEY` - `CLERK_SECRET_KEY` - `STRIPE_SECRET_KEY` - `STRIPE_WEBHOOK_SECRET` - `STRIPE_CONNECT_CLIENT_ID` - `MAPBOX_ACCESS_TOKEN` - `NEXT_PUBLIC_MAPBOX_TOKEN` - `CLOUDINARY_CLOUD_NAME` - `CLOUDINARY_API_KEY` - `CLOUDINARY_API_SECRET` - `NEXT_PUBLIC_POSTHOG_KEY` - `NEXT_PUBLIC_POSTHOG_HOST` - `SENTRY_DSN` - `SENTRY_AUTH_TOKEN` - `OPENAI_API_KEY`  Risk assessment:  - No committed real secrets were found in the repo areas inspected. - `.env.example` is good for names, but not enough for end-to-end local setup because Doppler/Vercel synchronization steps are not codified. - Runtime naming is clear. - Reproducibility is partial: a new machine can install and build, but cannot serve the app locally without separately obtaining Clerk and Supabase credentials. - Additional worker examples exist under `tools/*/.env.example`, which suggests environment handling is broader than the core app.  ## 12. Strengths  - `packages/db/migrations/*.sql`: strong canonical schema discipline with PostGIS support, enum modeling, and clear ownership rules. - `packages/core/src/validation.ts`: meaningful zod coverage for key create flows. - `packages/core/src/state-machines.ts`: explicit lifecycle control for bids, bookings, payments, opportunities, and inventory units. - `packages/services/src/discovery.ts`: typed wrappers over the search RPCs make the Phase 5 search layer real, not hypothetical. - `packages/services/src/bidding.ts`: sealed-bid and host-visibility rules are implemented in code, not only in docs. - `packages/services/src/payments.ts`: payment split and Stripe Connect destination-charge preparation are encapsulated and testable. - `docs/DECISIONS.md` and `docs/ROADMAP.md`: product and implementation assumptions are unusually well documented for this stage.  ## 13. Weaknesses  - `apps/web/app/page.tsx`: homepage is only a scaffold, not a marketplace surface. - `apps/web/app/dashboard/page.tsx`: dashboard stops at org context display and does not expose the implemented domain capabilities. - `apps/web/app/onboarding/page.tsx`: onboarding is functional but visually raw and not reusable. - `apps/web/app/globals.css`: no usable design system beyond background/foreground tokens. - `package.json`: root `test` script hides real package-level test health. - `.github/workflows/ci.yml`: CI verifies the placeholder root test instead of the actual unit suites. - `apps/web/middleware.ts`: deprecated Next middleware convention warning is already present under the pinned framework version. - `packages/db/src/types.ts`: hand-authored row types remain a maintenance risk until generation is wired.  ## 14. Product Gaps  Missing for a serious MVP:  - Map/list browse experience - Inventory unit detail page - CRUD UI for venues, opportunities, and inventory units - Bid submission flow - Host bid review and counter workflow UI - Booking confirmation and payment UI - Messaging and notifications - Reviews and verification flows - Admin/moderation tools - Public marketing/legal/support pages  The codebase already contains much of the domain logic required for these flows, but almost none of the user-facing implementation exists.  ## 15. Design System Gaps  - No shared `packages/ui` - No spacing, typography, radius, or shadow scale - No component variants - No responsive layout primitives - No icon implementation despite documented standard - No empty/loading/error states as patterns - No brand-level visual language yet  This should be standardized before product-surface expansion; otherwise every new page will compound the current inline-style debt.  ## 16. Architecture Risks  | Area | Risk | Impact | |---|---|---| | App architecture | UI layer is too thin relative to backend/domain work | Product may look stalled despite strong underlying foundation | | Data model | Hand-authored row types can drift from schema | Silent compile/runtime mismatch risk over time | | Auth/runtime | Hard runtime dependency on Clerk env in layout and middleware | New contributors cannot smoke-test locally without secrets | | CI | Placeholder root test script | False confidence from green CI | | Deployment | No workflow or infra config in repo | Hard to reason about release quality and repeatability | | UX | No responsive/component system | Fast feature work will create brittle UI debt | | Accessibility | Native-only forms, no systematic review | Future remediations become more expensive | | Security | Service role key powers server DB access; no route-layer authorization yet because API layer is absent | Fine for current limited surface, but risky when CRUD routes are added without a strong policy layer | | Scalability | Strong schema, but no generated types, no API boundaries, and no UI system | Operational scaling will be slower than domain scaling |  ## 17. Priority Roadmap  ### Immediate Fixes - Replace the root `test` script with real workspace test execution. - Update CI to run the actual package tests. - Document the exact Doppler or local env bootstrap needed for `pnpm dev`. - Migrate the deprecated Next middleware convention to the current proxy recommendation. - Decide whether pnpm build-script approval is required for local Supabase CLI usage.  ### Foundation Fixes - Generate Supabase DB types and stop hand-maintaining row contracts. - Create `packages/ui` and establish design tokens. - Add a shared app shell and page primitives. - Introduce route-level error/loading states. - Define route-handler/API conventions before feature UI work accelerates.  ### Product Buildout - Ship browse/map discovery surface on top of the existing search RPCs. - Ship opportunity and inventory unit CRUD UI. - Ship bid placement and host review workflows. - Ship payment and booking confirmation flows. - Add profile, messaging, and review surfaces.  ### Production Readiness - Add deploy workflows and smoke tests. - Add env management docs for staging/production. - Add observability wiring. - Add authorization policy checks for CRUD/API routes. - Add migration verification in CI and a release checklist.  ## 18. Recommended File/Folder Refactor  Recommended target shape, based on the current repo direction:  ```text apps/   web/     app/       (marketing)/       (app)/       api/     features/       auth/       orgs/       discovery/       inventory/       bids/       bookings/       payments/       admin/     components/       app-shell/       forms/       feedback/       navigation/ packages/   core/   db/   services/   ui/     src/tokens/     src/primitives/     src/patterns/ ```  Why: the repo already has clean domain boundaries. The missing step is a feature-oriented web layer and a real shared UI package.  ## 19. Recommended Domain Model  Recommendations only. These are proposed TypeScript interfaces derived from the existing canonical schema and service layer, not new product requirements.  ```ts export interface OrganizationSummary {   id: string;   name: string;   organizationType: "host" | "bidder" | "vendor" | "multi" | string;   verificationStatus: string;   stripeAccountId: string | null; }  export interface RoleProfileSummary {   id: string;   organizationId: string;   roleType: string;   displayName: string;   slug: string | null;   categoryTags: string[];   verificationStatus: string; }  export interface InventoryUnitSummary {   id: string;   opportunityId: string;   organizationId: string;   type: string;   name: string;   status: string;   commerceLayer: string | null;   minimumBidCents: number | null;   buyNowPriceCents: number | null;   reservePriceCents: number | null;   latitude: number | null;   longitude: number | null; }  export interface BidSummary {   id: string;   opportunityId: string;   inventoryUnitId: string | null;   bidderOrganizationId: string;   hostOrganizationId: string | null;   amountCents: number;   counterAmountCents: number | null;   status: string;   intendedUse: string | null; }  export interface BookingSummary {   id: string;   bidId: string;   inventoryUnitId: string;   bidderOrganizationId: string;   hostOrganizationId: string;   startsAt: string;   endsAt: string;   priceCents: number;   status: string; } ```  ## 20. Final Verdict  Current state: prototype.  Reasoning:  - It is not nonexistent. - It is not an empty shell because the schema, validation, service, and test layers are substantive. - It is not yet an MVP because core marketplace user flows are missing from the UI. - It is not beta or production-ready because runtime setup is not fully reproducible, CI test coverage is misleading, and deployment automation is absent.  Best next move: open a focused branch that fixes test orchestration and local setup first, then build the discovery and inventory surfaces directly on top of the existing service/domain packages.  What should not be built yet: advanced AI scoring, enterprise analytics, or broad product expansion beyond the first browse ΓåÆ unit detail ΓåÆ bid ΓåÆ booking flow.  What must be standardized before scaling: workspace test execution, generated DB types, a shared design/token system, route-level authorization conventions, and deployment/environment procedures.  ## Marketplace / Product Readiness Score  | Category | Score | Explanation | |---|---:|---| | Repo/GitHub alignment | 9/10 | Clean clone, good remote setup, aligned with `origin/main`; only missing branch discipline in practice if work continues on `main` | | Local setup quality | 6/10 | Install and build are easy; local runtime still requires undocumented secret provisioning | | Build health | 8/10 | Lint, typecheck, build, and package tests pass; runtime smoke still fails without env | | Product clarity | 8/10 | Docs clearly explain the product and roadmap | | Route/page completeness | 2/10 | Only auth, onboarding, and a skeletal dashboard exist | | Component maturity | 2/10 | No internal reusable UI library | | Design system maturity | 1/10 | Almost entirely absent | | Data model maturity | 9/10 | Strong schema and domain decisions | | Auth readiness | 7/10 | Clerk integration works structurally, but local runtime and role enforcement are incomplete | | Backend readiness | 7/10 | Service layer is real, but API surface is not exposed | | Payment readiness | 5/10 | Split logic and Stripe parameter prep exist; no live checkout surface or webhook flow | | Admin/moderation readiness | 2/10 | Schema exists; product surface does not | | Mobile UX | 1/10 | No mobile app package and no responsive web UI | | Accessibility | 2/10 | No systematic accessibility work is evident | | Security posture | 6/10 | Good auth baseline and server-side services; incomplete route-layer authorization and secret-dependent runtime | | CI/CD readiness | 5/10 | Basic CI exists, but it misses real tests and deployment | | Deployment readiness | 4/10 | Build artifact is healthy, but deploy path is mostly documentary | | Maintainability | 7/10 | Strong docs and domain boundaries; weak UI structure and manual DB types hold it back | | Scalability | 7/10 | Schema and service layering scale well; UI/API/platform ops do not yet | | Overall MVP readiness | 4/10 | Strong foundation, but too few product flows are live |
